/**
 * `@pipelex/sdk` — TypeScript SDK for the Pipelex hosted API.
 *
 * The client surface (`PipelexApiClient`, run lifecycle, product routes) lands
 * in a later phase. For now this barrel exposes the package version so the
 * build / typecheck / test pipeline has a real module to compile.
 */
export const SDK_VERSION = "0.1.0";
