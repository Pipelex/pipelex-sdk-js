// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: e4623057c69e8962796244a035d86e772ffd0231a8f4f6c1a4f31913db3f3c9b
// engine_version: 0.46.4
// projection: types / ts-zod
// options: {}
// content_hash: 456e6876433a7d7ffb727e5df27d78f01ca0444e7203d64aef92bd11e6272812
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import { TextSchema, type Text, CustomerSchema, type Customer } from "./types";

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}

/** Parse a wire payload into a validated `Customer`. */
export function parseCustomer(wire: unknown): Customer {
  return CustomerSchema.parse(wire);
}

/** Validate a `Customer` for the wire (keys are already wire-native). */
export function serializeCustomer(value: Customer): Customer {
  return CustomerSchema.parse(value);
}
